import Notification from './notification'
import { IMountOptions } from './types'

const config: IMountOptions = {
  height: 64,
  width: 328,
  offset: 16,
  zIndex: 999999,
  component: Notification
}

export default config
